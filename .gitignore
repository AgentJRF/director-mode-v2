# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — clé API IA (proxy local Claude vision)
.env
.env.*
!.env.example

# Images temporaires envoyées au CLI Claude Code (proxy IA)
.ai-tmp/

# NB: l'asset produit (public/asset/, © Adobe Inc.) ET la police Adobe Clean
# (public/fonts/*.otf, © Adobe Inc.) sont volontairement COMMITÉS dans ce dépôt
# PRIVÉ (choix utilisateur) pour que clone / StackBlitz / Codespaces marchent
# directement. scripts/fetch-asset.ps1 et fetch-fonts.ps1 restent dispo en fallback.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
